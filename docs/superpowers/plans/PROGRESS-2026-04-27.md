# 工作进展 — 2026-04-27

> 明天继续：直接查看本文档的「明天开始」部分即可。

## 今日完成

### 第一期：基本面因子引擎

**核心架构** `vnpy/alpha/factors/`：

```
vnpy/alpha/factors/
├── __init__.py           # 导出 FactorEngine, DataFetcher, FactorComputer, FactorStorage
├── base.py               # 3 个抽象基类（注册模式）
├── engine.py             # FactorEngine 总调度器（register → run_daily → get_snapshot）
├── fusion.py             # DimensionScorer (截面排名 0-100) + SignalFusion (加权融合)
├── fundamental/
│   ├── fetcher.py        # Tushare: income/fina_indicator/daily_basic/disclosure_date
│   ├── factors.py        # 7 个因子: revenue_yoy, profit_yoy, roe, gross_margin, debt_to_assets, pe_ttm, pb, ps_ttm
│   └── storage.py        # Parquet: fundamental_daily.parquet + fundamental_quarterly.parquet
├── flow/                 # [第二期]
│   ├── fetcher.py        # Tushare moneyflow_hsgt → 北向/南向累计 → 差分得日净流入
│   ├── factors.py        # 5d/10d/20d MA + sigmoid 情绪评分 (0-100)
│   └── storage.py        # Parquet: flow_daily.parquet
└── sentiment/            # [第三期占位]
    └── __init__.py
```

**Web 层** `web_app/`：

- `factor_api.py` — 4 个 API 端点: `/api/factors/{snapshot, history, detail, flow}`
- `app.py` — 注册 factor_bp Blueprint
- `scheduler_tasks.py` — 每日 15:35 定时拉取因子数据
- `templates/index.html` — 「前瞻指标」标签页（排名表 + 估值历史曲线 + 北向资金趋势图）

### 第二期：资金流向因子

**真实数据拉取**：通过 `moneyflow_hsgt` API 获取了近 3 个月（56 行）的北向资金数据

**融合权重**：`final_score = (fundamental*0.4 + flow*0.15) / 0.55`

技术难点解决：
- 市场级数据（flow）与 per-stock 数据（fundamental）合并：用 `join` 替代 `concat`，市场级数据广播到所有 symbols
- Chart.js v4 canvas 占用冲突：用 `Chart.getChart(canvas)` 替代局部变量检查
- 前端 CSS 显示问题：`display:none` 内联样式与 `.active` class 冲突

### 测试

`tests/test_factors.py` — 8 个单元测试全部通过：
- TestFactorEngine × 2
- TestDimensionScorer × 3
- TestSignalFusion × 1
- TestFundamentalStorage × 2

### Tushare API 权限现状

| API | 权限 | 用途 |
|-----|------|------|
| `daily` | ✅ 免费 | 日线行情 |
| `moneyflow_hsgt` | ✅ 免费 | 沪深港通资金流向（第二期用） |
| `daily_basic` | ❌ 需权限 | PE/PB/PS 估值（第一期用） |
| `income` | ❌ 需权限 | 利润表 |
| `fina_indicator` | ❌ 需权限 | ROE/毛利率 |
| `moneyflow` | ❌ 需权限 | 个股资金流向 |
| `margin` | ❌ 需权限 | 融资融券 |

**第一期当前用模拟数据跑通**，升级 Tushare 权限后：删除 `~/.vntrader/factors/fundamental_daily.parquet`，重跑 `run_daily()` 即可切换到真实数据。

---

## 明天继续

### 第三期：市场情绪因子

Tushare 免费层可用接口待确认：
- `stk_limit` — 涨跌停数据
- `limit_list_d` — 每日涨跌停列表
- `suspension_d` — 停复牌信息

如果可用，实现方向：
1. **涨停数/跌停数比** — 市场热度
2. **换手率异常** — `daily` API 的 `turnover_rate` 可直接用
3. **新高新低比** — `daily` API 可计算

否则用 `daily` API 的自有字段（`pct_chg`, `vol`, `amount`）构建替代情绪指标。

### 待优化

1. **真实数据接入**：获得 Tushare 权限后，在 Demo Data → Real Data 切换
2. **行业中性化**：基本面因子按行业分组标准化，避免行业偏见
3. **SignalFusion 接入 AlphaStrategy**：在回测中用 `MultiDimensionStrategy` 验证效果
4. **前端优化**：股票名称映射（目前显示 "-"）、日期选择器功能
5. **季频因子真实使用**：财报旺季时拉取 `income`/`fina_indicator` 数据

### 快速恢复工作环境

```bash
cd /Users/w4sh8899/project/vnpy

# 启动 Flask 服务
export TUSHARE_TOKEN=8338d9ae4c26c3ec32cffbd1b337d97228c22ba84cea0996410513bb
.venv/bin/python web_app/app.py

# 运行测试
.venv/bin/python -m pytest tests/test_factors.py -v

# ruff 检查
uv run ruff check vnpy/alpha/factors/ tests/test_factors.py
```

### Git 状态

```
共 22 个 commits 在 master 分支，领先 origin/master
需执行 git push 同步到远程
```
